import React from "react";
import { MoonLoader } from "react-spinners";

const Loader = () => {
  return <MoonLoader color="#4c031b" loading size={60} speedMultiplier={0.2} />;
};

export default Loader;
